The files in this directory are examples for the config files needed for irccurves.

While config.xml specifies the triggers to be loaded, log4j.xml covers the parameters
for Log4J as logging API.

In order for them to work, Both the config.xml and log4j.xml need to be put into the
home directory of irccurves and of course adapted.